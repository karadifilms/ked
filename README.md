Ked - Kannada Text Editor For English Keyboards 

#### TODO
- I'm using monospace, looks fine in chrome, firefox, edge. Let's see if we need to support more browser, but for now it's all good. 
- very difficult to remeber the case of all the letters
    - if letter is not case sensitive, treat both cases equally. 
- handling text efficiently w.r.t. DOM. (currently seems like it's not needed, handles the size of text that I use)
- prevLetterType can be an enum, does javascript have enum? 
- helper footer is bad, make it better